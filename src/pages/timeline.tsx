import type { NextPage } from 'next';
import Layout from '../components/Layout/Layout';

const Timeline: NextPage = () => (
  <Layout PageTitle="Timeline">
  <div>this pageg is timeline</div>
  </Layout>
);
export default Timeline;
